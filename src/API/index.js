import {
  signup,
  signin,
  getUserData,
  uploadProfilePic,
  getProfilePic,
} from "./user";
import { getMessages } from "./channel";

const api = {
  signup,
  signin,
  getUserData,
  uploadProfilePic,
  getMessages,
  getProfilePic,
};

export default api;
